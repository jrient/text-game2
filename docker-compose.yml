services:
  game:
    image: nginx:alpine
    container_name: pixel-survivor
    ports:
      - "8082:80"
    volumes:
      - ./game:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
